{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Main window capability with shell access for opening external URLs",
  "windows": ["main", "*"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "shell:default"
  ]
}
